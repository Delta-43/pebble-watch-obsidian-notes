# Copy to .env and fill in your own (gitignored, never committed). Referenced by CLAUDE.md/PLAN.md/
# TODO.md as $N8N_HOST — those dev-facing docs document the real reference deployment's specifics
# (matching the sibling project's own convention) without hardcoding them directly, since this repo is
# public.

# Your n8n host (the domain your real, already-running n8n instance is reachable at).
N8N_HOST=
